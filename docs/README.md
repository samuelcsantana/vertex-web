# docs

This folder is for supplementary documentation — architecture deep-dives, ADRs, runbooks, and similar
longer-form material — that Claude Code reads on demand when a task calls for it, rather than automatically
on every session the way `CLAUDE.md` and `AGENTS.md` are.
